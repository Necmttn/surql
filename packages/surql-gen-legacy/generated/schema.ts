/**
 * ⚠️ AUTO-GENERATED FILE ⚠️
 * This file is automatically generated. Do not modify it manually.
 * Any changes made to this file will be overwritten when regenerating.
 *
 * Generated by: @necmttn/surql
 * Date: 2025-05-13T11:14:10.468Z
 */

// Effect Schema Class API integration for SurrealDB types
import { Schema } from "effect";
import { RecordId, StringRecordId } from "surrealdb";

export const stringRecordIdSchema = <T extends string>(tableName: T) =>
  Schema.transform(
    recordIdLiteral(tableName),
    Schema.instanceOf(StringRecordId),
    {
      strict: true,
      decode: (from: `${T}:${string}`) => {
        return new StringRecordId(from);
      },
      encode: (to) => {
        return to.toString() as `${T}:${string}`;
      },
    },
  );

export const recordIdSchema = <T extends string>(tableName: T) =>
  Schema.transform(recordIdLiteral(tableName), Schema.instanceOf(RecordId), {
    strict: true,
    decode: (from: `${T}:${string}`) => {
      const [table, id] = from.split(":");
      if (!table || !id) {
        throw new Error(`Invalid RecordId: ${from} `);
      }
      return new RecordId(table, id);
    },
    encode: (to) => {
      return to.toString() as `${T}:${string}`;
    },
  });

export const recordIdLiteral = <T extends string>(tableName: T) =>
  Schema.TemplateLiteral(
    Schema.Literal(tableName),
    Schema.Literal(":"),
    Schema.String,
  );

/**
 * Create a RecordId schema for a specific table
 */
export function recordId<T extends string>(tableName: T) {
  return Schema.Union(
    recordIdSchema(tableName),
    stringRecordIdSchema(tableName),
  );
}

export namespace Account {
  export const Fields = {
    accountId: recordId("user"),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    id: Schema.String,
    password: Schema.String,
    providerId: Schema.String,
    updatedAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    userId: recordId("user"),
  };

  export class Account extends Schema.Class<Account>("Account")({
    ...Fields,
  }) {
    static readonly tableName = "account" as const;
  }

  export type Type = Schema.Schema.Type<typeof Account>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("account"),
  });
}

export namespace Block {
  export const Fields = {
    id: recordId("block"),
    content: Schema.Array(Schema.Unknown).annotations({
      description: "The content of the block.",
      default: [],
    }),
    created_at: Schema.DateFromSelf.annotations({
      description: "Timestamp when the block record was created.",
      surrealDefault: "time::now()",
    }),
    document: recordId("document").annotations({
      description: "The document this block is part of.",
    }),
    file: Schema.optional(
      recordId("file").annotations({
        description: "The source file this block originates from.",
      }),
    ),
    next: Schema.optional(
      recordId("block").annotations({
        description: "The next block in the document.",
      }),
    ),
    page: Schema.optional(
      Schema.Number.pipe(Schema.int()).annotations({
        description: "The page number where the block begins.",
      }),
    ),
    parent: Schema.optional(
      recordId("block").annotations({
        description: "The parent block in the document.",
      }),
    ),
    prev: Schema.optional(
      recordId("block").annotations({
        description: "The previous block in the document.",
      }),
    ),
    props: Schema.optional(
      Schema.Unknown.annotations({
        description:
          "Properties specific to the block type (e.g., heading level, URL).",
      }),
    ),
    type: Schema.String.annotations({
      description: "The type of structural block.",
    }),
    updated_at: Schema.DateFromSelf.annotations({
      description: "Timestamp when the block record was last updated.",
      surrealDefault: "time::now()",
    }),
    bbox: Schema.optional(
      Schema.Struct({
        b: Schema.Number.pipe(Schema.int()).annotations({
          description: "Bounding box: Bottom coordinate.",
        }),
        l: Schema.Number.pipe(Schema.int()).annotations({
          description: "Bounding box: Left coordinate.",
        }),
        r: Schema.Number.pipe(Schema.int()).annotations({
          description: "Bounding box: Right coordinate.",
        }),
        t: Schema.Number.pipe(Schema.int()).annotations({
          description: "Bounding box: Top coordinate.",
        }),
      }).annotations({
        description: "Bounding box of the block on its source page.",
      }),
    ),
  };

  export class Block extends Schema.Class<Block>("Block")({
    ...Fields,
  }) {
    static readonly tableName = "block" as const;
  }

  export type Type = Schema.Schema.Type<typeof Block>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("block"),
  });
}

export namespace Chat {
  export const Fields = {
    id: recordId("chat"),
    description: Schema.String,
    files: Schema.Array(recordId("file")),
    is_archived: Schema.Boolean.annotations({ default: false }),
    messages: Schema.Array(recordId("message")),
    owner: recordId("user"),
    project: Schema.optional(recordId("project")),
    started_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    team: recordId("team"),
    title: Schema.String,
    updated_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
  };

  export class Chat extends Schema.Class<Chat>("Chat")({
    ...Fields,
  }) {
    static readonly tableName = "chat" as const;
  }

  export type Type = Schema.Schema.Type<typeof Chat>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("chat"),
  });
}

export namespace Chunk {
  export const Fields = {
    block: recordId("block").annotations({
      description: "The structural block this chunk is part of.",
    }),
    content: Schema.String,
    document: recordId("document").annotations({
      description: "The logical document this chunk belongs to",
    }),
    embedding: Schema.optional(Schema.Array(Schema.Number)),
    end: Schema.Number.pipe(Schema.int()),
    enriched_content: Schema.String.annotations({
      description: "The content of the chunk after enrichment",
    }),
    enriched_text: Schema.String,
    file: Schema.optional(
      recordId("file").annotations({
        description:
          "Link to the original file record, if the document source was a file",
      }),
    ),
    id: Schema.String,
    important_kwd: Schema.Array(Schema.String),
    index: Schema.Number.pipe(Schema.int()),
    jina_embedding_v3: Schema.optional(
      Schema.Array(Schema.Number).annotations({
        description: "Jina embedding v3",
      }),
    ),
    metadata: Schema.Unknown,
    page: Schema.Number.pipe(Schema.int()),
    question_kwd: Schema.Array(Schema.String),
    start: Schema.Number.pipe(Schema.int()),
    bbox: Schema.optional(
      Schema.Struct({
        b: Schema.Number.pipe(Schema.int()).annotations({
          description: "Bottom",
        }),
        l: Schema.Number.pipe(Schema.int()).annotations({
          description: "Left",
        }),
        r: Schema.Number.pipe(Schema.int()).annotations({
          description: "Right",
        }),
        t: Schema.Number.pipe(Schema.int()).annotations({ description: "Top" }),
      }).annotations({ description: "Bounding box of the chunk" }),
    ),
  };

  export class Chunk extends Schema.Class<Chunk>("Chunk")({
    ...Fields,
  }) {
    static readonly tableName = "chunk" as const;
  }

  export type Type = Schema.Schema.Type<typeof Chunk>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("chunk"),
  });
}

export namespace Confluence {
  export const Fields = {
    id: recordId("confluence"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Confluence extends Schema.Class<Confluence>("Confluence")({
    ...Fields,
  }) {
    static readonly tableName = "confluence" as const;
  }

  export type Type = Schema.Schema.Type<typeof Confluence>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("confluence"),
  });
}

export namespace Contains {
  export const Fields = {
    id: recordId("contains"),
    in: Schema.instanceOf(RecordId).annotations({
      description: "Parent Block",
    }),
    order: Schema.Number.pipe(Schema.int()).annotations({
      description: "Order of the child block within its parent",
    }),
    out: recordId("block").annotations({ description: "Child Block" }),
  };

  export class Contains extends Schema.Class<Contains>("Contains")({
    ...Fields,
  }) {
    static readonly tableName = "contains" as const;
  }

  export type Type = Schema.Schema.Type<typeof Contains>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("contains"),
  });
}

export namespace Document {
  export const Fields = {
    id: recordId("document"),
    authors: Schema.Array(Schema.String).annotations({
      description: "Authors of the document",
    }),
    created_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    dates: Schema.Array(Schema.String),
    keywords: Schema.Array(Schema.String).annotations({
      description: "Keywords extracted from the document",
    }),
    name: Schema.String.annotations({
      description: "Display name or title of the document",
    }),
    organization: recordId("organization"),
    original_file: Schema.optional(
      recordId("file").annotations({
        description: "Link to the file record if source_type is ",
      }),
    ),
    original_last_updated_date: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    processed_at: Schema.optional(Schema.DateFromSelf),
    processing_error: Schema.optional(Schema.String),
    processing_status: Schema.String.annotations({ default: "pending" }),
    raw_content: Schema.optional(
      Schema.String.annotations({
        description: "Optional raw content before processing",
      }),
    ),
    source_id: Schema.String.annotations({
      description:
        "Unique identifier within the original source (e.g., file ID, Slack message ts, Notion page ID)",
    }),
    source_metadata: Schema.optional(
      Schema.Unknown.annotations({
        description:
          "Source-specific metadata (e.g., URL, channel, author, parent page)",
      }),
    ),
    source_type: Schema.String.annotations({
      description:
        "The type of the original source (e.g., file, slack, notion)",
    }),
    summary: Schema.optional(
      Schema.String.annotations({ description: "Summary of the document" }),
    ),
    team: recordId("team"),
    updated_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    output_paths: Schema.optional(
      Schema.Struct({
        artifacts: Schema.optional(
          Schema.String.annotations({
            description: "Path to other parser-specific artifacts.",
          }),
        ),
        html: Schema.optional(
          Schema.String.annotations({
            description: "Path to the HTML output from the parser.",
          }),
        ),
        json: Schema.optional(
          Schema.String.annotations({
            description: "Path to the JSON output from the parser.",
          }),
        ),
        markdown: Schema.optional(
          Schema.String.annotations({
            description: "Path to the Markdown output from the parser.",
          }),
        ),
        text: Schema.optional(
          Schema.String.annotations({
            description: "Path to the plain text output from the parser.",
          }),
        ),
      }).annotations({
        description: "Paths to various output artifacts from the parser.",
      }),
    ),
    parser: Schema.optional(
      Schema.Struct({
        settings: Schema.optional(
          Schema.Unknown.annotations({
            description: "Settings used for this parsing process.",
          }),
        ),
        type: Schema.String.annotations({
          description:
            "Identifier for the parser used (e.g., docling, marker_v1).",
        }),
        version: Schema.optional(
          Schema.String.annotations({
            description: "Version of the parser used.",
          }),
        ),
      }).annotations({ description: "Parser information" }),
    ),
  };

  export class Document extends Schema.Class<Document>("Document")({
    ...Fields,
  }) {
    static readonly tableName = "document" as const;
  }

  export type Type = Schema.Schema.Type<typeof Document>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("document"),
  });
}

export namespace File {
  export const Fields = {
    id: recordId("file"),
    created_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    document: Schema.optional(recordId("document")),
    extension: Schema.String,
    file_pages: Schema.Array(recordId("file_page")),
    name: Schema.String,
    orgSlug: Schema.String,
    organization: recordId("organization"),
    owningTeam: recordId("team"),
    path: Schema.String,
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this file",
      }),
    ),
    size: Schema.Number.pipe(Schema.int()),
    status: Schema.String.annotations({ default: "pending_upload" }),
    status_history: Schema.Array(Schema.Unknown).annotations({ default: [] }),
    teamSlug: Schema.String,
    type: Schema.String,
    updated_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    uploader: recordId("user"),
  };

  export class File extends Schema.Class<File>("File")({
    ...Fields,
  }) {
    static readonly tableName = "file" as const;
  }

  export type Type = Schema.Schema.Type<typeof File>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("file"),
  });
}

export namespace File_page {
  export const Fields = {
    id: recordId("file_page"),
    content: Schema.optional(Schema.String),
    error_message: Schema.optional(Schema.String),
    file: recordId("file"),
    image_path: Schema.optional(Schema.String),
    page_num: Schema.Number.pipe(Schema.int()).annotations({ default: 0 }),
    s3_error_path: Schema.optional(Schema.String),
    s3_input_path: Schema.optional(Schema.String),
    s3_output_path: Schema.optional(Schema.String),
    status: Schema.String.annotations({ default: "pending" }),
  };

  export class File_page extends Schema.Class<File_page>("File_page")({
    ...Fields,
  }) {
    static readonly tableName = "file_page" as const;
  }

  export type Type = Schema.Schema.Type<typeof File_page>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("file_page"),
  });
}

export namespace Github {
  export const Fields = {
    id: recordId("github"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Github extends Schema.Class<Github>("Github")({
    ...Fields,
  }) {
    static readonly tableName = "github" as const;
  }

  export type Type = Schema.Schema.Type<typeof Github>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("github"),
  });
}

export namespace Has_next {
  export const Fields = {
    id: recordId("has_next"),
    in: recordId("chunk"),
    out: recordId("chunk"),
  };

  export class Has_next extends Schema.Class<Has_next>("Has_next")({
    ...Fields,
  }) {
    static readonly tableName = "has_next" as const;
  }

  export type Type = Schema.Schema.Type<typeof Has_next>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("has_next"),
  });
}

export namespace Invitation {
  export const Fields = {
    id: recordId("invitation"),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    email: Schema.String,
    expiresAt: Schema.DateFromSelf,
    inviterId: recordId("user"),
    organizationId: recordId("organization"),
    role: Schema.String,
    status: Schema.String,
    teamId: Schema.optional(recordId("team")),
  };

  export class Invitation extends Schema.Class<Invitation>("Invitation")({
    ...Fields,
  }) {
    static readonly tableName = "invitation" as const;
  }

  export type Type = Schema.Schema.Type<typeof Invitation>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("invitation"),
  });
}

export namespace Jira {
  export const Fields = {
    id: recordId("jira"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Jira extends Schema.Class<Jira>("Jira")({
    ...Fields,
  }) {
    static readonly tableName = "jira" as const;
  }

  export type Type = Schema.Schema.Type<typeof Jira>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("jira"),
  });
}

export namespace Links_to {
  export const Fields = {
    id: recordId("links_to"),
    in: recordId("block"),
    out: recordId("block"),
  };

  export class Links_to extends Schema.Class<Links_to>("Links_to")({
    ...Fields,
  }) {
    static readonly tableName = "links_to" as const;
  }

  export type Type = Schema.Schema.Type<typeof Links_to>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("links_to"),
  });
}

export namespace Member {
  export const Fields = {
    id: recordId("member"),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    organizationId: recordId("organization"),
    role: Schema.String,
    teamId: Schema.optional(recordId("team")),
    userId: recordId("user"),
  };

  export class Member extends Schema.Class<Member>("Member")({
    ...Fields,
  }) {
    static readonly tableName = "member" as const;
  }

  export type Type = Schema.Schema.Type<typeof Member>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("member"),
  });
}

export namespace Message {
  export const Fields = {
    id: recordId("message"),
    attachments: Schema.Array(Schema.Unknown),
    chat: recordId("chat"),
    content: Schema.String,
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    parts: Schema.Array(Schema.Unknown),
    role: Schema.String,
    user: recordId("user"),
  };

  export class Message extends Schema.Class<Message>("Message")({
    ...Fields,
  }) {
    static readonly tableName = "message" as const;
  }

  export type Type = Schema.Schema.Type<typeof Message>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("message"),
  });
}

export namespace Note {
  export const Fields = {
    id: recordId("note"),
    created_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
    updated_at: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
  };

  export class Note extends Schema.Class<Note>("Note")({
    ...Fields,
  }) {
    static readonly tableName = "note" as const;
  }

  export type Type = Schema.Schema.Type<typeof Note>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("note"),
  });
}

export namespace Notion {
  export const Fields = {
    id: recordId("notion"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Notion extends Schema.Class<Notion>("Notion")({
    ...Fields,
  }) {
    static readonly tableName = "notion" as const;
  }

  export type Type = Schema.Schema.Type<typeof Notion>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("notion"),
  });
}

export namespace Onedrive {
  export const Fields = {
    id: recordId("onedrive"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Onedrive extends Schema.Class<Onedrive>("Onedrive")({
    ...Fields,
  }) {
    static readonly tableName = "onedrive" as const;
  }

  export type Type = Schema.Schema.Type<typeof Onedrive>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("onedrive"),
  });
}

export namespace Organization {
  export const Fields = {
    id: recordId("organization"),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    logo: Schema.optional(
      Schema.String.annotations({
        description: "The logo of the organization",
      }),
    ),
    metadata: Schema.optional(
      Schema.Unknown.annotations({
        description: "Additional metadata for the organization",
      }),
    ),
    name: Schema.String,
    slug: Schema.String,
    updatedAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
  };

  export class Organization extends Schema.Class<Organization>("Organization")({
    ...Fields,
  }) {
    static readonly tableName = "organization" as const;
  }

  export type Type = Schema.Schema.Type<typeof Organization>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("organization"),
  });
}

export namespace Part_of {
  export const Fields = {
    id: recordId("part_of"),
    in: recordId("chunk"),
    out: recordId("document"),
  };

  export class Part_of extends Schema.Class<Part_of>("Part_of")({
    ...Fields,
  }) {
    static readonly tableName = "part_of" as const;
  }

  export type Type = Schema.Schema.Type<typeof Part_of>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("part_of"),
  });
}

export namespace Project {
  export const Fields = {
    id: recordId("project"),
    chats: Schema.Array(recordId("chat")),
    description: Schema.String,
    name: Schema.String,
    organization: recordId("organization"),
    team: recordId("team"),
  };

  export class Project extends Schema.Class<Project>("Project")({
    ...Fields,
  }) {
    static readonly tableName = "project" as const;
  }

  export type Type = Schema.Schema.Type<typeof Project>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("project"),
  });
}

export namespace Session {
  export const Fields = {
    activeOrganizationId: Schema.optional(
      Schema.String.annotations({
        description: "The id of the active organization",
      }),
    ),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    expiresAt: Schema.String,
    id: Schema.String,
    ipAddress: Schema.String,
    token: Schema.String,
    updatedAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    userId: recordId("user"),
  };

  export class Session extends Schema.Class<Session>("Session")({
    ...Fields,
  }) {
    static readonly tableName = "session" as const;
  }

  export type Type = Schema.Schema.Type<typeof Session>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("session"),
  });
}

export namespace Slack {
  export const Fields = {
    id: recordId("slack"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Slack extends Schema.Class<Slack>("Slack")({
    ...Fields,
  }) {
    static readonly tableName = "slack" as const;
  }

  export type Type = Schema.Schema.Type<typeof Slack>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("slack"),
  });
}

export namespace Team {
  export const Fields = {
    id: recordId("team"),
    createdAt: Schema.DateFromSelf.annotations({
      surrealDefault: "time::now()",
    }),
    default: Schema.Boolean.annotations({
      default: "ON team TYPE bool DEFAULT false",
    }),
    isPrivate: Schema.Boolean.annotations({ default: false }),
    name: Schema.String,
    organizationId: recordId("organization"),
    slug: Schema.String,
    updatedAt: Schema.optional(
      Schema.DateFromSelf.annotations({ surrealDefault: "time::now()" }),
    ),
  };

  export class Team extends Schema.Class<Team>("Team")({
    ...Fields,
  }) {
    static readonly tableName = "team" as const;
  }

  export type Type = Schema.Schema.Type<typeof Team>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("team"),
  });
}

export namespace Text_input {
  export const Fields = {
    id: recordId("text_input"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Text_input extends Schema.Class<Text_input>("Text_input")({
    ...Fields,
  }) {
    static readonly tableName = "text_input" as const;
  }

  export type Type = Schema.Schema.Type<typeof Text_input>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("text_input"),
  });
}

export namespace User {
  export const Fields = {
    id: recordId("user"),
    chats: Schema.Array(recordId("chat")),
    organizations: Schema.Array(recordId("member")),
  };

  export class User extends Schema.Class<User>("User")({
    ...Fields,
  }) {
    static readonly tableName = "user" as const;
  }

  export type Type = Schema.Schema.Type<typeof User>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("user"),
  });
}

export namespace Vote {
  export const Fields = {
    id: recordId("vote"),
    chatId: recordId("chat"),
    isUpvoted: Schema.Boolean,
    messageId: recordId("message"),
  };

  export class Vote extends Schema.Class<Vote>("Vote")({
    ...Fields,
  }) {
    static readonly tableName = "vote" as const;
  }

  export type Type = Schema.Schema.Type<typeof Vote>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("vote"),
  });
}

export namespace Web_page {
  export const Fields = {
    id: recordId("web_page"),
    document: Schema.optional(recordId("document")),
    preferred_document: Schema.optional(
      recordId("document").annotations({
        description:
          "Reference to the document record considered the primary/best output for this source entity",
      }),
    ),
  };

  export class Web_page extends Schema.Class<Web_page>("Web_page")({
    ...Fields,
  }) {
    static readonly tableName = "web_page" as const;
  }

  export type Type = Schema.Schema.Type<typeof Web_page>;

  export const update = Schema.Struct({
    ...Object.fromEntries(
      Object.entries(Fields).map(([key, schema]) => [
        key,
        Schema.optional(schema as Schema.Any),
      ]),
    ),
    id: recordId("web_page"),
  });
}
